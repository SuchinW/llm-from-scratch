# utils

Small shared helpers. Keep this folder small on purpose - when you're tempted to put "shared logic" here, first ask whether it belongs in the component that needs it.

Typical contents: data loading, config dataclasses, simple logging, a `set_seed` helper, parameter-counting utilities.
